import sys

lines = []

for line in sys.stdin:
 	lines.append(line.rstrip())

vals = lines[0].split(",")
for i in range(0, len(vals)):
	vals[i] = int(vals[i])

def numList(num):
	myList = []

def prog1(valls):
	iterator = 0

	while True:
		#process first opcode


def prog2():
	pass

print("Result 1: " + str(prog1(vals[:])))
print("Result 2: " + str(prog2()))
